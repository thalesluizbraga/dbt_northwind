# Extensões do docker-compose gerado pelo Astro CLI.
# Monta o projeto dbt local dentro do container para hot-reload (sem rebuild).

x-airflow-common: &airflow-common
  extra_hosts:
    # Permite que containers acessem serviços no host (Postgres Northwind em :5433)
    - "host.docker.internal:host-gateway"
  volumes:
    # Espelha 4-airflow/dbt_northwind → path usado em dags/dag.py (DBT_PROJECT_PATH)
    - ./dbt_northwind:/usr/local/airflow/dbt_northwind:ro

services:
  scheduler:
    <<: *airflow-common
  dag-processor:
    <<: *airflow-common
  triggerer:
    <<: *airflow-common
  api-server:
    <<: *airflow-common
